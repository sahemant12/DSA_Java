package com.Hemantsah;

public class Important {
    public static void main(String[] args) {
        //1. Arrays.copyOfRange(a, 1, a.length)
        //2. return new int[] {-1,-1}; //returning array
        //3. queue.poll(): to remove the element and retrieve the top of the queue and queue.offer(): to add an element in queue
        //4. ansList.add(0,levelList); --> move forward the 0th index (AND) list.set(0, last); --> replace the 0th index
    }
}
